{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/eclipse-dataspaceconnector.com/contract-offer.schema.json",
  "title": "Contract Offer",
  "description": "A contract offer",
  "type": "object",
  "properties": {
    "id": {
      "description": "The unique identifier for the contract offer",
      "type": "string"
    },
    "policy": {
      "$ref": "https://www.w3.org/ns/odrl.jsonld#Set"
    },
    "asset": {
      "$ref": "../asset/asset.schema.json"
    },
    "provider": {
      "type": "string",
      "format": "uri"
    },
    "consumer": {
      "type": "string",
      "format": "uri"
    },
    "offerStart": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time",
      "description": "The offer start time expressed as milliseconds since the Unix epoch"
    },
    "offerEnd": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time",
      "description": "The offer expiration time expressed as ISO 8601 date time."
    },
    "contractStart": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time",
      "description": "The contract start time expressed as an ISO 8601 date time."
    },
    "contractEnd": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time",
      "description": "The contract expiration time expressed as an ISO 8601 date time."
    },
    "contractDuration": {
      "type": [
        "string",
        "null"
      ],
      "format": "duration",
      "description": "The contract duration time expressed as an ISO 8601 duration. The contract end date takes precedence. If no duration and no end date are specified, "
    },
    "properties": {
      "$ref": "../common/definitions.schema.json#/definitions/extensibleMap"
    }
  }
}
