{
  "definitions": {
    "extensibleMap": {
      "type": "object",
      "additionalProperties": {
        "type": "object"
      },
      "description": "Defines a map data structure for storing extensible properties"
    }
  }
}
